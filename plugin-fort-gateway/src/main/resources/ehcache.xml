<config
        xmlns:xsi='http://www.w3.org/2001/XMLSchema-instance'
        xmlns='http://www.ehcache.org/v3'
        xsi:schemaLocation="http://www.ehcache.org/v3 http://www.ehcache.org/schema/ehcache-core.xsd">
    <!-- 持久化路径 -->
    <persistence directory="/tmp/ehcacheData"/>

    <!-- 缓存模版,此处为了显示其用法,也可以不用模版直接在cache中配置与模版参数相同 -->
    <cache-template name="template">
        <key-type>java.lang.String</key-type>
        <value-type>java.lang.String</value-type>
        <expiry>
            <!-- 单位默认为秒当用秒作单位时,可以不填-->
            <tti unit="hours">1</tti>
        </expiry>
        <resources>
            <!-- 单位默认为entries当用entries作单位时,可以不填-->
            <heap>200</heap>
            <offheap unit="MB">5</offheap>
            <!-- persistent 默认为false可以不填-->
            <disk unit="MB">50</disk>
        </resources>
    </cache-template>

    <!--登录验证码缓存-->
    <cache alias="captchaSessionCache1" uses-template="template">
        <expiry>
            <!--此处会覆盖模版中的(TTL)配置 -->
            <tti>300</tti>
        </expiry>
    </cache>
    <!--登录信息-->
    <cache alias="oauthSessionCache2" uses-template="template"/>
</config>